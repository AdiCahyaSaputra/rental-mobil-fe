{
  "extends": "next/core-web-vitals",
  "ignorePatterns": ["node_modules/**", ".git/**", ".next/**"],
  "rules": {
    "quotes": ["error", "single", {
      "allowTemplateLiterals": true,
    }],
    "semi": ["error", "never"],
  }
}
